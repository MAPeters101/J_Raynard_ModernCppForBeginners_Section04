#include <iostream>

using namespace std;
/*
int main() {
	int change{ 0 };
	cout << "Please enter the amount of change: ";
	cin >> change;
	int remaining{ change };
	int ndollars = remaining / 100;
	if (ndollars) {
		cout << ndollars << " dollars" << endl;
	}
	remaining %= 100;
	int nhalves = remaining / 50;
	if (nhalves) {
		cout << nhalves << " halves" << endl;
	}
	remaining %= 50;
	int nquarters = remaining / 25;
	if (nquarters) {
		cout << nquarters << " quarters" << endl;
	}
	remaining %= 25;
	int ndimes = remaining / 10;
	if (ndimes) {
		cout << ndimes << " dimes" << endl;
	}
	remaining %= 10;
	int nnickles = remaining / 5;
	if (nnickles) {
		cout << nnickles << " nickles" << endl;
	}
	remaining %= 5;
	int npennies = remaining;
	if (npennies) {
		cout << npennies << " pennies" << endl;
	}



	return 0;
}
*/

